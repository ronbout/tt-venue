jQuery(document).ready(function () {
	// code for the login page, such as validation
	jQuery("#wp-submit").addClass("btn btn-primary");
	jQuery("#wp-submit").removeClass("button button-primary");
});
